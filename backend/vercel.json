{
    "buildCommand": "pip install -r requirements.txt",
    "functions": {
        "api/*.py": {
            "memory": 2048,
            "maxDuration": 60
        }
    },
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": "/api/index"
        }
    ]
}